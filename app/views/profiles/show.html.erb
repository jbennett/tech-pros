<% content_for :title_text do %>
  <%= @profile.name %>
<% end %>

<p class="-mt-4 mb-4 text-blue-400"><%= link_to 'Back Home', root_path %></p>

<div class=""><%= @profile.long_description %></div>

<% unless @profile.tags.blank? %>
  <div class="mx-2 mt-4 mb-3 flex flex-wrap items-baseline justify-center">
    <%= render partial: 'dashboards/tag', collection: @profile.tags %>
  </div>
<% end %>
