
<div class="mb-4 flex rounded-full border-2 border-gray-600 overflow-hidden">
  <input class="flex-grow py-1 px-2 outline-none focus:shadow-outline focus:bg-blue-100" type="search" />
  <button class="px-4 bg-gray-600 text-white">
    Search
  </button>
</div>

<%= render partial: 'entity', collection: @entities  %>
<%= will_paginate @entities %>
